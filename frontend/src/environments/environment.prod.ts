// Archivo de configuración para el entorno de producción
export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000/api'  // Cambiar a la URL de producción cuando sea necesario
};
